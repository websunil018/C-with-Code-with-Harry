#include <stdio.h>

int main()
{
   // label:
       // printf("We are inside label");
      // goto end;
   // printf("Hello world\n");
   // goto label;  
 //   end:
     //   printf("We are end");
      int num;
     for(int i = 0; i < 8; i++)
     {
        peintf("%d", i);
        for(int j +0; j < 8; j++)
        {
             printf("Enter the numer. enter 0 to exit\n");
             scanf("%d", %num);
        }
     }


    return 0;


}